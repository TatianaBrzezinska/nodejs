// Поставимо значення 10
let x = 10;

// Давайте додамо + 1
x++;

// Выводімо в консоль
console.log(x);


// Змінна для зберігання дати та часу коли наш юзер залогувався в системі
let dtSys1 = new Date(); // створення об'єкта дати

// Виклик функції для обробки даних, якщо дані існують
if (data !== undefined) {
    process(data); // викликаємо процес обробки даних
}
